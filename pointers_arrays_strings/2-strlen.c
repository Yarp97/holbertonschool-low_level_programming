#include <string.h>
#include <stdio.h>
#include "main.h"

/**
 *_strlen - function that returns the length of a string.
 *@s: String
 *Return: number
 */

int _strlen(char *s)
{
int length;
length = strlen(s);
return (length);
}
