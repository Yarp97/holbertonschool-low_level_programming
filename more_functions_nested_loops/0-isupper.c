#include "main.h"
#include <stdio.h>

/**
 *_isupper - checks for uppercase character.
 *@c: letter
 *Return: letter
 */

int _isupper(int c)
{

if (c >= 97 && c <= 122)
{
return (0);
}
else
return (1);

}
